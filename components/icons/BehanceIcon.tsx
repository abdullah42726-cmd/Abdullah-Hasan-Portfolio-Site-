import React from 'react';

const BehanceIcon: React.FC<{ className?: string }> = ({ className }) => (
    <i className={`fi fi-brands-behance ${className}`} aria-hidden="true"></i>
);

export default BehanceIcon;
